---
name: ""
overview: ""
todos: []
isProject: false
---

# Plan: UX/UI-Analyse & Vorschläge – alle Fenster (Activities/Screens)

Erstellt am: 2026-02-12_12-35

## Anlass / Frage (Original)

„Das gleiche Frage, prüfe für alle fenstern im das app. Dann gib mir genau so ein detailiertes plan. Und dein ergebnisse mit meine frage gleich in eine datei nehmen. In jedem bereich in plan datei schreiben: welche funktion oder um welche fenster es handelt. Dann konkrete vorschläge.“  
Hinweis: **Nur Analyse/Design-Vorschläge**, keine Code-Änderungen.

## Kurz-Prinzipien (global, für alle Screens)

- **Klarer Primär-CTA pro Screen** (1 Hauptaktion, Rest sekundär).
- **Aktive Filter/Modi immer sichtbar** (z. B. Chips in „collapsed state“).
- **Zustände sauber**: Loading / Empty / Error jeweils mit 1 sinnvoller Aktion (Retry, Reset Filter, Create).
- **Konsistente Navigation**: Back-Icon in TopAppBar überall gleich; Titel + Kontextinfo (z. B. Kunde/Liste) klar.
- **Kontext-spezifische Inhalte**: Tabs/Listen zeigen je Screen das, was dort „Arbeit“ ist (nicht überall identisch).
- **Performance/Skalierung**: bei 500 Kunden Liste/Filter/Sortierung so gestalten, dass „finden“ schnell ist (auch ohne Paging).

---

## 1) Login – `LoginActivity` (Compose: `LoginScreenContent`)

**Funktion:** Anonymer Login, Fehleranzeige + Retry.

**Konkrete Vorschläge**

- **Besseres Feedback beim Login**: Button disabled + Label „Anmelden…“, statt Screen komplett zu wechseln.
- **Fehlertext konkretisieren** (Netzwerk vs. Auth) und 1 klare Aktion („Erneut versuchen“).
- **Offline-Hinweis** direkt im Login (weil Offline im Projekt relevant ist): „Ohne Internet ist Anmeldung nicht möglich“.

---

## 2) Hauptmenü – `MainActivity` / `MainScreen`

**Funktion:** Einstieg; Tour-Planner als Hero; Admin: Kunden/Neu Kunde + 2×2 Kacheln + Slot-Vorschläge.

**Konkrete Vorschläge**

- **Kachel-Hierarchie**: Tour Hero bleibt top, aber „Kunden“ sollte als zweiter „Primary“-Pfad sehr klar sein (z. B. gleiche Gewichtung wie Tour-Hero-Button).
- **Admin vs. Nicht-Admin**: Für Nicht-Admin wirkt der Screen sehr leer. Vorschlag: auch ohne Admin die wichtigsten Navigationen sichtbar (zumindest „Kunden“ + „Tour Planner“).
- **Slot-Vorschläge (Ad-hoc)**:
  - als eigene Card-Liste mit „Warum sehe ich das?“ (z. B. „Ad-hoc Kunden: nächste möglichen Termine“).
  - Aktion pro Vorschlag: „Öffnen“ (Kundendetail) + optional „Termin anlegen“ Shortcut.
- **Offline/Sync-Badges**: gut vorhanden; Verbesserung: Badge klickbar → kleine Erklärung (BottomSheet) „Was bedeutet Sync?“

---

## 3) Kundenübersicht – `CustomerManagerActivity` / `CustomerManagerScreen`

**Funktion:** Kundenliste mit Tabs **Gewerblich/Privat/Tour**, Suche + Filter, Admin: Bulk/Export/Neu.

**Konkrete Vorschläge**

- **Tabs**
  - Tab-Titel mit **Counts/Badges** („Privat (87)“), Count auf gefilterter Menge.
  - „Tour“ Label prüfen → ggf. **„Tour-Kunden“** (klarer).
- **Suche + Filter**
  - Aktive Filter als Chips sichtbar, auch wenn Filter eingeklappt (mit „×“ + „Filter zurücksetzen“).
  - Schnellfilter (KundenTyp, Pausiert, Ohne Tour, Keine Termine) stets sichtbar; Rest in BottomSheet.
  - UI-Hinweis zur Logik: „Keine Termine“ schließt „Ohne Tour“ aus.
  - Sortierung ergänzen: „Nächster Termin / Überfällig zuerst / Stadt/PLZ / (Tour) Wochentag/Zeitfenster“.
- **Karten**
  - Status-Badges: **PAUSIERT / OHNE TOUR / KEINE TERMINE / AUF ABRUF**.
  - 1 Zeile „Nächster relevanter Termin“ (statt nur A/L Wochentag-Text).
  - Karte als Ganzes klickbar (Bulk: Checkbox separat).
- **Bulk/Export**
  - Bulk als klarer Modus: TopBar zeigt „X ausgewählt“, „Alle gefilterten auswählen“, „Auswahl löschen“.
  - Export: UI klären ob Export „alle“ oder „gefiltert“ ist; ideal: Auswahl/Preview.

---

## 4) Kunde anlegen – `AddCustomerActivity` / `AddCustomerScreen` (Form: `CustomerStammdatenForm`)

**Funktion:** Neuer Kunde; Name Pflicht; Save; Duplikat-Dialog.

**Konkrete Vorschläge**

- **TopBar Navigation**: Back-Icon fehlt (navigationIcon leer). Konsistenten Back-Button hinzufügen.
- **Form-Progress**: Bei vielen Feldern (Adresse/Telefon/Tags/TourSlot etc.) in Abschnitte gliedern („Stammdaten“, „Tour“, „Intervalle/Termine“).
- **Pflichtfelder klar**: „Name *“ sichtbar; inline Validation statt Toast-only.
- **Duplikat-Dialog UX**: Vorschlag: im Dialog gleich 1–2 ähnliche Treffer anzeigen („Meintest du …?“) mit „Öffnen“ statt nur Ja/Abbrechen.

---

## 5) Kundendetail – `CustomerDetailActivity` / `CustomerDetailScreen` (Tabs: Stammdaten / Termine / Belege)

**Funktion:** Anzeigen/Bearbeiten, Fotos, Urlaub, Termine-Regeln, Belege.

**Konkrete Vorschläge**

- **Tab-Layout**:
  - Tab-Titel mit Counts/Badges (z. B. „Belege (12)“ / „Termine (… )“).
  - Default-Tab abhängig vom Entry: wenn aus TourPlanner → direkt „Termine“.
- **Edit-Modus Sichtbarkeit**: „Edit“ sollte den ganzen Screen deutlich in Bearbeitungsmodus versetzen (Banner/Chip „Bearbeiten aktiv“).
- **Termine-Tab**
  - „Neuer Termin“ als Primary CTA; die Auswahl (Regelmäßig/Monatlich/Einmalig/Urlaub) ist gut, aber als BottomSheet mit kurzen Erklärtexten („Wann nutzen?“).
  - Tour-Kontext sichtbar: „Gehört zu Tour-Liste …“ prominent, plus „TourSlot fehlt“ als Warnung mit CTA „TourSlot setzen“.
- **Belege-Tab**
  - Schnellaktionen „Neue Erfassung“ konsolidieren (3 Optionen ok, aber als ein Button → Auswahlsheet).

---

## 6) Tourenplaner – `TourPlannerActivity` / `TourPlannerScreen`

**Funktion:** Tagesansicht mit Sektionen (Überfällig/Heute/Erledigt), Aktionen-Sheet, Overview-Dialog, Karte, Drag&Drop-Reihenfolge.

**Konkrete Vorschläge**

- **TopBar Dichte**: Viele Aktionen (Prev/Next/Heute/Map/Refresh/Erledigt). Vorschlag: Sekundäraktionen in Overflow (⋮), Primary: Datum + Heute + Map.
- **Sektionen**:
  - Sektion-Header mit Counts, „Expand/Collapse all“.
  - Filter direkt im TourPlanner: nach Kundenart/Liste/Stadt (nur UI/Design-Vorschlag; technisch später).
- **Customer-Karten im Planer**
  - Statusbadge bereits vorhanden → zusätzlich 1 Zeile „A/L heute“ bzw. „nächste Aktion“.
  - Telefon/Navi als Quick-Actions (Icon-Reihe), aber mit ausreichend Touch-Targets.
- **Erledigt-Sheet**
  - Klarer „Undo“-Pfad (Rückgängig) und Bestätigungstexte kurz + eindeutig.

---

## 7) Kunden-Listen Übersicht – `KundenListenActivity` / `KundenListenScreen`

**Funktion:** Listen anzeigen, suchen, sortieren (Name vs Count), löschen, neu, refresh.

**Konkrete Vorschläge**

- **Doppelte „Neu Liste“ Aktion**: Es gibt FAB + Button im Header. Vorschlag: eine klare Primary (FAB) und Header nur „Refresh“ oder umgekehrt.
- **Leerer Zustand**: derzeit Emojis („📋“, „⚠️“). Für konsistentes Branding: Material Icon + Text + CTA „Liste erstellen“.
- **Sortierung**: gut mit Chips. Ergänzung: „Wochentag“/„Listenart“ als Filterchip.
- **List-Card Inhalte**: pro Liste kurz zeigen: Art (G/P/Tour), Wochentag (falls), Anzahl Kunden, nächster Termin.

---

## 8) Liste erstellen – `ListeErstellenActivity` / `ListeErstellenScreen`

**Funktion:** Name, Art (Gewerbe/Privat/Tour), optional Wochentag, speichern.

**Konkrete Vorschläge**

- **Inline-Fehler statt Toast**: Name fehlt → direkt am TextField Fehlertext.
- **Tour vs Wochentag**: Die Logik „Tour-Liste = kein Wochentag 0..6“ ist kompliziert. UI sollte das vereinfachen:
  - Auswahl „Liste ist: Wochentag-Liste / Tour-Liste“ als klare Toggle.
  - Danach passende Felder ein-/ausblenden.
- **Nach Save**: Erfolgsfeedback im Screen (SnackBar) statt nur delay+finish.

---

## 9) Liste bearbeiten – `ListeBearbeitenActivity` / `ListeBearbeitenScreen`

**Funktion:** Metadaten bearbeiten, Listen-Termine, Kunden in Liste hinzufügen/entfernen, Refresh, Delete.

**Konkrete Vorschläge**

- **Informationsarchitektur**: Drei klare Sektionen (Metadaten / Termine / Kunden) mit sticky Überschriften oder Accordion (Teilweise vorhanden: Collapsible).
- **Kunden hinzufügen**: Suchfeld + Filter nach Kundenart; „bereits in Liste“ klar markieren.
- **Termine-Bereich**: CTA „Termin hinzufügen“ + „Termine löschen“ getrennt und mit klarer Bestätigung („X Termine werden entfernt“).
- **Edit-Modus**: Banner „Bearbeiten aktiv“ + „Abbrechen/Übernehmen“ als feste Bottom-Bar.

---

## 10) Erfassung-Menü – `ErfassungMenuActivity` / `ErfassungMenuScreen`

**Funktion:** Einstieg: Erfassung starten, Belege.

**Konkrete Vorschläge**

- **Beschreibende Untertexte**: unter Buttons 1 Zeile („Wäschepositionen erfassen“ / „Belege ansehen & erledigen“).
- **Dritter Pfad** (optional): „Artikel/Preise“ verlinken, weil Nutzer oft im selben Kontext arbeitet (nur UX-Idee).

---

## 11) Waschen/Erfassung – `WaschenErfassungActivity` / `WaschenErfassungScreen` (State-Machine)

**Funktion:** Multi-Flow: Kunde suchen → Belegliste → Erfassen/Formular → Details.

**Konkrete Vorschläge**

- **Flow-Orientierung**: Oben ein kleiner „Breadcrumb/Step“ („Kunde > Belege > Erfassen“) oder zumindest Titel dynamisch je State.
- **Back-Verhalten**: ist state-basiert gut; UX: Back-Icon in TopBar muss das reflektieren (statt generischem „zurück“).
- **Neue Erfassung (3 Varianten)**: als 1 Primary Button „Neue Erfassung“ → Auswahlsheet (Kamera/Formular/Manuell) + kurze Erklärung.
- **Kunde suchen**: bei leerer Query currently Hinweise; zusätzlich „Letzte Kunden“ oder „Favoriten“ (UX-Idee, falls sinnvoll).
- **Preise-Hint**: „Allgemeine Preise“ Hinweis sollte als Info-Icon mit erklärendem Text sein (nicht nur Textblock).

---

## 12) Belege (global) – `BelegeActivity` / `BelegeScreen`

**Funktion:** Alle Belege (über Kunden), Kunde suchen, Belegliste, Detail, erledigt-tab.

**Konkrete Vorschläge**

- **IA**: „Alle Belege“ als Default ist gut. Ergänzung: Filterchips „Offen/Erledigt“, Zeitraum (Monat) und Volltextsuche.
- **Kunde-Suche**: gleiche UI wie WaschenErfassung (wird genutzt) – gut. Ergänzung: Trefferanzahl + Empty-State mit „Filter löschen“.
- **Beleg-Detail**: Actions (Löschen/Erledigt) klar und konsistent (TopBar oder BottomBar), plus „Undo“ nach Erledigt via Snackbar.

---

## 13) Preise – `PreiseActivity` / `PreiseScreen`

**Funktion:** Navigation zu Kundenpreise / Tour-Preisliste / Artikelverwaltung.

**Konkrete Vorschläge**

- **Kontext-Text**: Kurz erklären, was der Unterschied ist („Kundenpreise überschreiben Standardpreise“ / „Tour-Preisliste = Fallback“).
- **Reihenfolge**: Häufigkeit: Artikel → Tourpreise → Kundenpreise (oder nach euren Workflows).
- **Suche** (optional): Direktsuche nach Artikel/Kunde, die in die passenden Screens springt.

---

## 14) Kundenpreise – `KundenpreiseActivity` / `KundenpreiseScreen`

**Funktion:** Kunde wählen, dann Preisliste pro Artikel (Netto/Brutto).

**Konkrete Vorschläge**

- **Kunden-Kontext**: Im List-State (Preise) oben zusätzlich Kundendaten (Adresse/Typ) klein anzeigen.
- **Sort/Filter**: nach Artikelname, Preisabweichung, Kategorie/Einheit.
- **Empty-State**: „Keine Kundenpreise“ + CTA „Tour-Preise prüfen“ oder „SevDesk Preise importieren“ (wenn passend).

---

## 15) Tour-Preisliste – `TourPreislisteActivity` / `TourPreislisteScreen`

**Funktion:** Liste von Tour-Preisen, Add-Dialog (Artikel suchen, Netto/Brutto), Delete.

**Konkrete Vorschläge**

- **Add-Dialog**: Sehr funktional. UX-Verbesserung:
  - Suchergebnisliste: deutlicher „Auswählen“-State (Checkmark + sticky selected).
  - Validierung: Dezimal-Input mit Formatierung/Hinweis (z. B. „12,50“).
  - Nach Save: Snackbar „Preis gespeichert“ + optional „Weiteren hinzufügen“.
- **Liste**: Suchfeld oben (bei vielen Artikeln), optional Filter „ohne Preis“ / „mit Preis“.

---

## 16) Artikelverwaltung – `ArtikelVerwaltungActivity` / `ArtikelVerwaltungScreen`

**Funktion:** Artikel-Liste, Delete (Icon ist im Screen disabled, Activity erlaubt Delete via Dialog).

**Konkrete Vorschläge**

- **Inkonsistenz**: Screen hat Delete-Button disabled → wirkt kaputt. UX-Plan: Delete entweder überall aktiv oder gar nicht zeigen.
- **Suche + Sort**: Suchfeld + Sort nach Name/Einheit; ggf. Gruppierung nach Einheit.
- **Empty-State**: CTA „Import (SevDesk)“ oder „Artikel anlegen“ (falls in Zukunft).

---

## 17) Einstellungen – `SettingsActivity` / `SettingsScreen`

**Funktion:** Preise, Data Import, Reset App Data, Abmelden; Menü (⋮) für Reset.

**Konkrete Vorschläge**

- **Back-Icon**: navigationIcon ist leer → konsistenten Back-Button ergänzen.
- **Gefährliche Aktionen**: Reset/Abmelden visuell trennen (Section „Gefahr“), rote Akzentfarbe, klarer Erklärungstext.
- **SevDesk Readonly Hint**: gut; optional als „Info-Card“ mit Icon.

---

## 18) Data Import – `DataImportActivity` / `DataImportScreen`

**Funktion:** Einstieg zu SevDesk Import.

**Konkrete Vorschläge**

- **Mehr Kontext**: Kurz erklären, was importiert wird (Kontakte/Artikel/Kundenpreise) und dass es read-only ist.
- **Letzter Import Status** (UX-Idee): „Zuletzt importiert: …“ (wenn Daten vorhanden).

---

## 19) SevDesk Import – `SevDeskImportActivity` / `SevDeskImportScreen`

**Funktion:** Token verwalten; Import Kontakte/Artikel/Preise; Delete/Reset Reimport; Busy-Indicator.

**Konkrete Vorschläge**

- **Token-Handling**: Token-Feld als „Password-like“ (maskiert, mit Eye-Toggle), plus „Test token“ (nur UX-Idee).
- **Busy-State**: Buttons disabled ist gut; zusätzlich klare Statuszeile „Importiere Kontakte…“.
- **Destruktive Buttons** (Delete/Clear): visuell als „danger“ (rote Outline) + Erklärtext.
- **Ergebnis-Feedback**: Nach Import: Anzahl importierter/übersprungener Elemente anzeigen (UI).

---

## 20) Statistik – `StatisticsActivity` / `StatisticsScreen`

**Funktion:** Kennzahlen Cards; Loading/SleepMode/Error.

**Konkrete Vorschläge**

- **Informationsdichte**: Cards sind gut; Ergänzung: kleine Sektionen („Heute“, „Woche/Monat“, „Kundenbestand“).
- **Erklärtexte**: „Erledigungsquote heute“ kurz erklären (Definition).
- **Interaktion** (UX-Idee): Tip auf Card → Drilldown-Liste (z. B. welche Kunden sind überfällig).

---

## 21) Karte – `MapViewActivity` / `MapViewScreen`

**Funktion:** Öffnet extern Google Maps, entweder mit Adressliste (TourPlanner) oder lädt Kunden und öffnet dann Maps.

**Konkrete Vorschläge**

- **„Trampolin“-Screen**: UI ist quasi Loading/Empty/Error. Verbesserung:
  - Wenn Adressen übergeben: sofortige „Öffne Google Maps…“ + Cancel (1–2 Sekunden), statt sofort finish.
  - Wenn gefiltert-to-today: im UI kurz erklären, bevor Maps öffnet (Transparenz).
- **Empty-State**: konkrete Anleitung „Keine Adressen gefunden – bitte Kundenadresse pflegen.“

---

## 22) Urlaub – `UrlaubActivity` / `UrlaubScreen`

**Funktion:** Urlaubseinträge pro Kunde, hinzufügen/bearbeiten/löschen.

**Konkrete Vorschläge**

- **Kundenkontext** oben anzeigen (Name + Typbadge), damit klar ist, für wen Urlaub gesetzt wird.
- **Datumsauswahl**: nach Auswahl „von/bis“ ein Review-Dialog ist gut; zusätzlich „Ganzer Monat“-Quick-Pick (UX-Idee).
- **Empty-State**: CTA „Urlaub hinzufügen“ prominent.

---

## 23) Ausnahme-Termine / Einmalige Termine – `AusnahmeTerminActivity`

**Funktion:** Kalender-UI; Terminart-Auswahl; optional „A + automatische Lieferung“.

**Konkrete Vorschläge**

- **Terminart zuerst** (UX): erst „Was willst du anlegen?“ (A/L/A+L), dann Datum wählen → reduziert Dialogkaskaden.
- **Kundenkontext**: Name/Adresse oben anzeigen (aktuell wird Kunde geladen, aber UI zeigt ihn nicht).
- **Bestätigung**: Klarer Text „Lege Abholung am … an (Lieferung automatisch …)“.

---

## 24) Unregelmäßige Termin-Vorschläge – `TerminAnlegenUnregelmaessigActivity`

**Funktion:** Slot-Vorschläge (2/8 Wochen), Multi-Select, Termine anlegen.

**Konkrete Vorschläge**

- **TopBar**: Titel + Back-Icon + Kontext (Kunde).
- **Multi-Select UX**: Statt „✓“ Text: Checkbox oder Chip „Ausgewählt“.
- **Sticky Bottom-Bar**: Button „X Termine anlegen“ fix unten (ist quasi geplant via bottom padding).
- **Erklärung**: Warum 2 vs 8 Wochen (kundentypabhängig) als kurzer Hint.

---

## Offene Punkte / Nächste Schritte (ohne Umsetzung)

- Priorisieren nach Nutzerrolle: **Fahrer (TourPlanner/Map), Dispo (Kunden/Listen), Büro (Erfassung/Belege/Preise)**.
- Pro Screen 1–2 Änderungen auswählen, damit UX konsistent wächst (statt viele kleine, inkonsistente Tweaks).

